PROCESS:

1. Domains
    Hook the domain display:
        Action buttons:
            Scan provider for Domains
            Provider dropdown
        Domain Row:
            DNS provider (eg. IONOS)
            DNS Status (blank, Active, A_C Synced, All Synced, Error)
            Action button: Unsync, Resync, Back sync
2. Provider credentials for customer
    Auth type: Authorization, Bearer, X-API-KEY
    Value
    Provider API definition (someday - admin only)